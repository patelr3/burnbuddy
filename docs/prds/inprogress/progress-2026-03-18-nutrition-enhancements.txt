# Ralph Progress Log — Nutrition Section Enhancements
Started: 2026-03-18
---

## Codebase Patterns
- Shared package must be built (`cd packages/shared && yarn build`) before API/web typecheck passes — they resolve from dist/
- Web pages at `apps/web/src/app/(main)/nutrition/recipes/` use `@burnbuddy/shared` imports for types and constants
- API route validation pattern: check input → return 400 with descriptive error → proceed with business logic
- API test pattern: vi.hoisted mocks + supertest + buildApp() — no emulators
- Auth cookie `auth_session` bypasses Next.js middleware for dev-browser testing
- Ingredient form fields (quantity/unit) only appear after selecting a food from USDA search
- yarn install needed before typecheck in fresh worktrees

---

## 2026-03-18T06:55 - US-001
- Implemented: Replace ingredient unit text field with dropdown
- Files changed:
  - `packages/shared/src/nutrition.ts` — Added SUPPORTED_UNITS constant and IngredientUnit type
  - `apps/web/src/app/(main)/nutrition/recipes/new/page.tsx` — Replaced text input with <select> dropdown
  - `apps/web/src/app/(main)/nutrition/recipes/[id]/page.tsx` — Same dropdown replacement on edit page
  - `services/api/src/routes/nutrition.ts` — Added unit validation to POST/PUT recipe endpoints
  - `services/api/src/routes/nutrition.test.ts` — Added 2 unit validation tests
- **Learnings for future iterations:**
  - Ingredient type uses `IngredientUnit | string` union to allow existing non-standard units to display
  - SUPPORTED_UNITS is exported as `as const` for type narrowing
  - Both recipe new/edit pages have nearly identical ingredient forms — changes must be applied to both
  - Food search requires API running on port 3021 to work in browser testing
  - Pre-existing typecheck errors in mobile workspace are unrelated — use per-package typecheck
---

## 2026-03-18T07:04 - US-002
- Implemented: Add supplement type definitions and catalog
- Files changed:
  - `packages/shared/src/nutrition.ts` — Added Supplement type, SupplementEntry type, COMMON_SUPPLEMENTS constant (8 supplements)
- **Learnings for future iterations:**
  - Supplement nutrients reference existing NutrientId values (vitaminD, vitaminC, calcium, iron, vitaminB12, zinc, magnesium, omega3)
  - Vitamin D IU to mcg conversion: 1000 IU = 25 mcg (the SUPPORTED_NUTRIENTS unit for vitaminD is mcg)
  - Fish Oil omega3 amount is in grams (matching SUPPORTED_NUTRIENTS unit for omega3)
  - COMMON_SUPPLEMENTS uses isCustom: false; custom user supplements will use isCustom: true
  - SupplementEntry stores supplementName (not supplementId) so entries remain readable even if catalog changes
---
