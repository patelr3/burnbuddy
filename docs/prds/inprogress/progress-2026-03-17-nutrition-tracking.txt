# Ralph Progress Log — Nutrition Tracking
Started: 2026-03-17
---

## Codebase Patterns
- Shared package types go in individual files under `packages/shared/src/` and are re-exported from `index.ts`
- After changing shared package, run `cd packages/shared && yarn build` before API/web typecheck
- API tests: 503 tests across 24 files, all passing after US-003
- Firestore mock pattern: hoisted mockDocSnap (mutable `.exists`), mockDocRef (`.get()`/`.set()`), mockGetDb chain
- `food-search-cache.ts` wraps USDA calls with Firestore caching — use `cachedSearchFoods`/`cachedGetFoodDetails` in routes
- Node 24 has native `fetch` — use vi.stubGlobal('fetch', mockFn) to mock it in tests
- USDA nutrient mapping is in `services/api/src/services/usda-food-search.ts` — USDA_NUTRIENT_MAP constant
- Need `yarn install` in worktree before typecheck works (node_modules may not be present)

## 2026-03-17T05:50 - US-001
- What was implemented: Added nutrition domain types to `@burnbuddy/shared`
- Files changed:
  - `packages/shared/src/nutrition.ts` (new) — NutrientId, NutrientInfo, NutrientAmount, Ingredient, Recipe, MealEntry, NutritionGoals, DailyNutritionSummary, FoodSearchResult types + SUPPORTED_NUTRIENTS constant
  - `packages/shared/src/index.ts` — added `export * from './nutrition'`
- **Learnings for future iterations:**
  - Shared package compiles to `dist/` via `tsc`; web/API resolve from compiled output
  - Pre-existing API typecheck fails without `yarn install` (missing @types/node, express types)
  - All 464 API tests pass at baseline — use as regression check
---

## 2026-03-17T05:53 - US-002
- What was implemented: USDA FoodData Central integration service with searchFoods() and getFoodDetails() functions
- Files changed:
  - `services/api/src/services/usda-food-search.ts` (new) — Service with USDA API integration, nutrient ID mapping, search and detail endpoints
  - `services/api/src/services/usda-food-search.test.ts` (new) — 19 unit tests covering nutrient mapping, search, details, error handling
- **Learnings for future iterations:**
  - Node 24 has native `fetch` — no need for node-fetch or axios dependencies
  - USDA FoodData Central search endpoint: `/v1/foods/search?query=...&api_key=...`
  - USDA food detail endpoint: `/v1/food/{fdcId}?api_key=...`
  - Omega-3 is split across 3 USDA nutrient IDs (ALA=1404, EPA=1278, DHA=1272) — must sum them
  - Search results have `foodNutrients[].nutrientId` + `.value`; detail results have `foodNutrients[].nutrient.id` + `.amount` — two different extraction functions needed
  - vi.stubGlobal('fetch', mockFn) works cleanly for mocking native fetch in vitest
  - Full test suite now has 483 tests across 23 files
---

## 2026-03-17T05:56 - US-003
- What was implemented: Firestore food search cache wrapping USDA API calls with 30-day TTL
- Files changed:
  - `services/api/src/services/food-search-cache.ts` (new) — `cachedSearchFoods()` and `cachedGetFoodDetails()` functions with Firestore `foodCache` collection
  - `services/api/src/services/food-search-cache.test.ts` (new) — 20 unit tests covering cache hit, miss, expired, error fallback, key normalization
- **Learnings for future iterations:**
  - Cache keys: normalized query (lowercase, trimmed, collapsed whitespace) for search, `fdc_{fdcId}` for details
  - Empty USDA results are NOT cached (prevents caching transient failures)
  - Cache read/write failures are non-fatal — always fall back to USDA API
  - Firestore mock pattern: hoisted mockDocSnap with mutable `.exists`, mockDocRef with `.get()` and `.set()`, mockGetDb returning chain
  - Full test suite now has 503 tests across 24 files
---

## 2026-03-17T05:59 - US-004
- What was implemented: Recipe CRUD API routes (POST, GET list, GET by ID, PUT, DELETE) in `services/api/src/routes/nutrition.ts`
- Files changed:
  - `services/api/src/routes/nutrition.ts` (new) — Express router with 5 recipe endpoints, `calculateNutrientsPerServing` helper
  - `services/api/src/routes/nutrition.test.ts` (new) — 32 unit tests covering all CRUD operations, auth, ownership validation, nutrient calculation
- **Learnings for future iterations:**
  - Recipe ownership checks return 404 (not 403) to avoid leaking recipe existence to non-owners
  - `directNutrients` takes precedence over ingredient-calculated nutrients in GET /:id (nutrientsPerServing field)
  - `calculateNutrientsPerServing` exported for potential reuse in meal logging
  - DELETE returns 204 with empty body via `res.status(204).send()`
  - Full test suite now has 535 tests across 25 files
---

## 2026-03-17T06:04 - US-005
- What was implemented: Meal logging API routes (POST, GET by date, DELETE) in existing nutrition router
- Files changed:
  - `services/api/src/routes/nutrition.ts` — Added 3 meal endpoints, `resolveMealNutrients` helper, `evaluateNutritionPoints` stub
  - `services/api/src/routes/nutrition.test.ts` — Added 20 meal logging tests (auth, validation, CRUD, recipe nutrient resolution)
- **Learnings for future iterations:**
  - Meal routes share the same router as recipe routes (`/nutrition/meals` and `/nutrition/recipes`)
  - `resolveMealNutrients` fetches recipe when recipeId is provided, scales nutrients by servingsConsumed
  - Fire-and-forget pattern: `evaluateNutritionPoints(uid, date).catch(err => logger.error(...))`
  - `evaluateNutritionPoints` is a stub returning Promise.resolve() — US-007 will implement the real logic
  - Collection `mealEntries` uses compound query `uid == X AND date == Y` with `orderBy('createdAt', 'asc')`
  - Test mock pattern: separate hoisted mocks for each Firestore collection (mockMealsDocGet vs mockRecipesDocGet)
  - Full test suite now has 555 tests across 25 files
---

## 2026-03-17T06:08 - US-006
- What was implemented: Goals, summary, and food search API routes in existing nutrition router
- Files changed:
  - `services/api/src/routes/nutrition.ts` — Added GET/PUT /goals, GET /summary, GET /foods/search endpoints
  - `services/api/src/routes/nutrition.test.ts` — Added 21 new tests (goals CRUD, validation, summary calculation, food search proxy)
- **Learnings for future iterations:**
  - Goals stored in `nutritionGoals` collection with doc ID = uid (single doc per user, upsert via `.set()`)
  - Summary aggregates all meals for a date across all SUPPORTED_NUTRIENTS, calculates percentComplete with Math.round
  - Points earned count comes from `nutritionPoints` collection (will be populated by US-007)
  - Food search proxies through `cachedSearchFoods` from food-search-cache service
  - Mock pattern for new collections: add hoisted mocks for doc/query, add collection routing in `getDb` mock, re-wire in beforeEach
  - Full test suite now has 576 tests across 25 files
---

## 2026-03-17T06:13 - US-007
- What was implemented: Nutrition points awarding service — evaluates daily nutrient intake against goals, awards/revokes points
- Files changed:
  - `services/api/src/services/nutrition-points.ts` (new) — `evaluateNutritionPoints()` function with Firestore award tracking + monthlyPoints increment/decrement
  - `services/api/src/services/nutrition-points.test.ts` (new) — 14 unit tests covering award, revoke, no-double-award, error handling, multi-nutrient, sum-across-meals
  - `services/api/src/routes/nutrition.ts` — Replaced stub with import of real `evaluateNutritionPoints` from service
  - `services/api/src/routes/nutrition.test.ts` — Added hoisted mock for `../services/nutrition-points` to prevent .catch() error after resetAllMocks
- **Learnings for future iterations:**
  - When extracting a fire-and-forget function from route into separate service, the route test needs a hoisted mock (not inline `vi.fn()`) because `resetAllMocks()` clears inline mock implementations, causing `.catch()` to fail on undefined
  - `nutritionPointsAwarded` collection uses doc ID `${uid}_${date}_${nutrientId}` for idempotent award tracking
  - `FieldValue.increment(-1)` works for decrementing points — same pattern as increment
  - Award tracking doc contains `{ uid, date, nutrientId, awardedAt }` — minimal for lookups
  - Full test suite now has 590 tests across 26 files
---
