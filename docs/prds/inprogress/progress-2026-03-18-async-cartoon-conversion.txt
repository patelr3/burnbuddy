# Ralph Progress Log — Async Cartoon Conversion & Larger Upload Support
Started: 2026-03-18

## Codebase Patterns
- Sharp mock chain in tests needs all chained methods (rotate, resize, jpeg/webp, toBuffer) as vi.fn().mockReturnThis()
- Blob paths use format `profile-pictures/${uid}/original.jpeg` and `profile-pictures/${uid}/avatar.jpeg`
- Test file: `users-profile-picture.test.ts` — 22 tests covering upload, format validation, cartoon conversion, passthrough
- Build shared package before API typecheck: `cd packages/shared && yarn build`
- Run API tests with: `cd services/api && npx vitest run <test-name>`
- Use `FieldValue` from `firebase-admin/firestore` (not `admin.firestore.FieldValue`) — the import works in tests without mocking
- Delete test file: `users-delete-profile-picture.test.ts` — 4 tests covering delete endpoint

---

## 2026-03-18T06:26 - US-001
- Implemented: Update image format to JPEG and increase file size limit to 15 MB
- Files changed:
  - `services/api/src/routes/users.ts` — multer limit 5→15 MB, sharp .webp()→.jpeg({quality:90}), blob paths .webp→.jpeg, content types
  - `services/api/src/routes/users-profile-picture.test.ts` — updated all webp refs to jpeg, 5 MB→15 MB test
  - `apps/web/src/lib/api.ts` — error message 5→15 MB
  - `apps/mobile/src/lib/api.ts` — error message 5→15 MB
- **Learnings for future iterations:**
  - The sed bulk replace of `image/webp` to `image/jpeg` also changed input file contentTypes in tests (e.g., WebP upload test). Be careful to preserve input format test data when changing output format.
  - The sharp mock chain must include the new `.jpeg()` method alongside the old `.webp()` mock
  - Delete endpoint also references blob paths (original.webp, avatar.webp) — don't forget those
---

## 2026-03-18T06:33 - US-002
- Implemented: Add cartoon conversion status tracking (profilePictureStatus field)
- Files changed:
  - `packages/shared/src/types.ts` — Added `profilePictureStatus?: 'processing' | 'ready' | 'failed' | null` to `UserProfile`
  - `services/api/src/routes/users.ts` — Upload handler sets 'processing' after original upload, 'ready' after cartoon completion; delete handler clears profilePictureStatus
  - `services/api/src/routes/users-profile-picture.test.ts` — Updated Firestore assertions: now expects 2 update calls (processing + ready)
  - `services/api/src/routes/users-delete-profile-picture.test.ts` — Removed `admin.firestore.FieldValue` mock, use real `FieldValue` import; fixed blob paths from .webp to .jpeg; verify profilePictureStatus deletion
- **Learnings for future iterations:**
  - `admin.firestore.FieldValue` from the mock doesn't work in profile picture tests — use `FieldValue` from `firebase-admin/firestore` import instead (already imported in users.ts)
  - The delete test file had stale `.webp` blob paths from US-001 — always check sibling test files when changing formats
  - When adding a Firestore write step, existing tests that count `mockUsersDocUpdate` calls need updating
---
