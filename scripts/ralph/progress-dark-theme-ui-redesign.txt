# Ralph Progress Log
Started: Sat Mar  7 07:53:26 PM UTC 2026
---

## Codebase Patterns
- Tailwind v4 uses `@theme` block in globals.css for CSS custom properties (--color-*) which auto-generate utility classes
- Both globals.css `@theme` and tailwind.config.ts colors must stay in sync
- Must run `cd packages/shared && yarn build` before web typecheck — API/web resolve @burnbuddy/shared from compiled dist/
- Pre-existing mobile typecheck failures (expo tsconfig) are unrelated to web changes — check web typecheck independently
- NavBar is in `apps/web/src/app/(main)/layout.tsx` — do NOT import NavBar in individual page components
- Pages are split into `(auth)` (login/signup, no NavBar) and `(main)` (all authenticated pages, with NavBar) route groups

## 2026-03-07T19:55 - US-001
- Updated globals.css @theme block with Apple Fitness dark palette: primary=#30D158, secondary=#0A84FF, surface=#1C1C1E, surface-elevated=#2C2C2E, accent-pink=#FF2D55
- Updated tailwind.config.ts with matching color definitions
- Set body background to black, default text to white
- Files changed: `apps/web/src/app/globals.css`, `apps/web/tailwind.config.ts`
- **Learnings for future iterations:**
  - `yarn typecheck` runs all workspaces — mobile has pre-existing errors. Use `cd apps/web && yarn typecheck` for web-only checks.
  - Shared package must be built first (`cd packages/shared && yarn build`) before web typecheck passes.
  - Tailwind v4 @theme block and tailwind.config.ts must both be updated for colors.
---

## 2026-03-07T19:57 - US-002
- Updated root layout body classes: `min-h-screen bg-black text-white antialiased`
- Created `(auth)` route group with simple passthrough layout (no NavBar) for login/signup
- Created `(main)` route group with NavBar + `pb-20 md:pb-0` bottom padding for all authenticated pages
- Moved login/ and signup/ into `(auth)/`, all other pages into `(main)/`
- api/ route stays at root level (not affected by route groups)
- Files changed: `apps/web/src/app/layout.tsx`, `apps/web/src/app/(auth)/layout.tsx`, `apps/web/src/app/(main)/layout.tsx`, plus file moves
- **Learnings for future iterations:**
  - Next.js route groups `(name)` don't affect URLs — only organizational for layout separation
  - NavBar is now in `(main)/layout.tsx`, not individual pages — future stories should NOT add NavBar to page components
  - Auth pages (login/signup) are in `(auth)/` group and will never see the NavBar
  - Middleware paths (`/login`, `/signup`) are unaffected by route group renames
---

## 2026-03-07T20:03 - US-003
- Redesigned NavBar component with responsive desktop top bar / mobile bottom tabs
- Desktop: dark sticky top bar with logo, nav links (Home/Friends/Account), Sign Out button
- Mobile: fixed bottom bar with SVG icon tabs and pill-shaped green active state
- Removed NavBar imports from all 8 page components (page.tsx, account, friends, burn-buddies/[id], burn-buddies/new, burn-squads/[id], burn-squads/new, profile/[uid])
- Removed fragment wrappers (`<>...</>`) from pages that no longer need them
- Files changed: `apps/web/src/components/NavBar.tsx`, plus 8 page files under `(main)/`
- **Learnings for future iterations:**
  - NavBar uses `isActive()` helper: exact match for `/`, prefix match for other routes
  - SVG icons are inline components (HomeIcon, FriendsIcon, AccountIcon) accepting `active` prop
  - Mobile nav uses `fixed bottom-0` with `z-50`, desktop uses `sticky top-0` with `z-40`
  - `bg-black/90 backdrop-blur-md` gives the glassmorphism effect on both bars
  - When removing fragment wrappers from pages, must remove both opening `<>` and closing `</>`
---

## 2026-03-07T20:20 - US-004
- Applied dark theme styling to entire home page (page.tsx)
- DashboardSkeleton: `bg-gray-200` → `bg-gray-800`, `border-slate-100` → `border-gray-700`
- Start Workout button: `bg-orange-500` → `bg-primary` (green), `hover:bg-green-600`
- Active Workout banner: yellow theme → `bg-surface` with `border-accent-pink`, pink text
- Workout type selector modal: white bg → `bg-surface`, green active state, dark inputs
- Buddy/squad list items: `bg-surface border-gray-700` cards, white text, green burn streak
- Pending requests: dark cards with subtle translucent colored borders
- Section headers: `text-white` instead of `text-gray-500`
- Burn streak emoji: `text-primary` (green) instead of `text-orange-500`
- Files changed: `apps/web/src/app/(main)/page.tsx`
- **Learnings for future iterations:**
  - Use theme tokens (`bg-primary`, `bg-surface`, `bg-surface-elevated`, `text-primary`, `bg-secondary`) instead of hardcoded colors
  - Translucent color overlays (e.g., `bg-amber-500/20`, `border-secondary/30`) work well for subtle dark-theme accents
  - `hover:bg-surface-elevated` provides a natural hover state on `bg-surface` cards
---

## 2026-03-07T20:21 - US-005
- Updated StatCard component for dark theme: `bg-white` → `bg-surface`, `border-gray-200` → `border-gray-700`, default value color `text-gray-900` → `text-white`
- Labels already `text-gray-400` — no change needed
- colorClass logic preserved for colored stat values
- Files changed: `apps/web/src/components/StatCard.tsx`
- **Learnings for future iterations:**
  - StatCard is a simple presentational component — dark theme changes are just class swaps
  - Browser verification requires auth; login page confirms dark theme globals work but can't verify authenticated components without credentials
  - Default fallback color (`colorClass ?? 'text-white'`) ensures StatCards without explicit colors still look correct on dark backgrounds
---

## 2026-03-07T20:25 - US-006
- Updated GettingStartedCard for dark theme: `bg-green-50` → `bg-surface`, `border-slate-200` → `border-gray-700`
- Text colors: body text `text-gray-300`, strong tags `text-primary` (green), close button `text-gray-400 hover:text-gray-200`
- Heading explicitly `text-white`
- Files changed: `apps/web/src/components/GettingStartedCard.tsx`
- **Learnings for future iterations:**
  - GettingStartedCard is a simple presentational component — dark theme is straightforward class swaps
  - Use `text-primary` on `<strong>` tags for emphasis on dark backgrounds instead of default bold
---

## 2026-03-07T20:26 - US-007
- Applied dark theme to Account page: all sections use `bg-surface border-gray-700`, headings `text-white`, descriptions `text-gray-400`
- AccountSkeleton: `bg-gray-200` → `bg-gray-800`, `border-gray-200` → `border-gray-700`
- Input fields: `bg-surface-elevated border-gray-600 text-white placeholder-gray-500`
- Buttons: green hover states (`hover:bg-green-600`), remove photo button dark styled
- Sign-out section: `bg-red-50 border-red-200` → `bg-surface border-red-500/30`
- Purple upload text lightened for dark bg (`text-purple-400`)
- NavBar already removed in US-003 — no changes needed
- Files changed: `apps/web/src/app/(main)/account/page.tsx`
- **Learnings for future iterations:**
  - For dark sign-out sections, use translucent red border (`border-red-500/30`) on `bg-surface` instead of red background
  - Purple text needs lightening on dark backgrounds (`text-purple-600` → `text-purple-400`)
  - Account page had no NavBar import to remove — US-003 already handled it
---

## 2026-03-07T20:28 - US-008
- Applied dark theme to entire Friends page
- FriendsSkeleton: `bg-gray-200` → `bg-gray-800`, `border-gray-100` → `border-gray-700`
- Page header: `text-gray-900` → `text-white`, Add Friend button hover green
- Search panel: `bg-gray-50 border-gray-200` → `bg-surface border-gray-700`, input `bg-surface-elevated`
- Search results: `bg-white` → `bg-surface`, `hover:bg-gray-50` → `hover:bg-surface-elevated`
- Confirmation dialogs (friend + burn buddy): `bg-white` → `bg-surface border-gray-700`, cancel buttons dark styled
- Pending requests: dark cards with translucent badge backgrounds (`bg-green-500/20`, `bg-gray-700`)
- Friends list: `bg-surface` cards, `hover:bg-surface-elevated`, translucent borders for action buttons
- Burn Buddy/Pending badges: `bg-primary/20` and `bg-yellow-500/20` instead of light-mode pastels
- Remove button: `border-red-500/30 hover:bg-red-500/10` for subtle dark accents
- NavBar already removed in US-003 — no changes needed
- Files changed: `apps/web/src/app/(main)/friends/page.tsx`
- **Learnings for future iterations:**
  - Translucent color overlays (`bg-color/20`, `border-color/30`, `hover:bg-color/10`) are the dark-theme equivalent of pastel backgrounds
  - Modal dialogs need `border border-gray-700` on dark theme for definition against `bg-black/50` overlay
  - `text-yellow-600` → `text-yellow-400` for readability on dark backgrounds
---

## 2026-03-07T20:30 - US-009
- Applied dark theme to all 5 Burn Buddy/Squad detail and create pages plus Profile page
- **burn-buddies/[id]**: Skeleton `bg-gray-800`, header/borders `border-gray-700`, badge `bg-amber-500/20 text-amber-400`, edit panel `bg-surface`, schedule display `border-primary/30 bg-primary/10 text-primary`, workout log dark borders/text
- **burn-buddies/new**: Header dark, error banner `bg-red-500/10 border-red-500/30`, friend selection `border-primary bg-primary/20`, confirmation dialog `bg-surface border-gray-700`
- **burn-squads/[id]**: Skeleton dark, header with `bg-violet-500/20 text-violet-400` badge, edit panel dark with violet accents, delete confirmation `bg-surface border-red-500/30`, member list dark borders, admin badge `bg-amber-500/20`, workout log dark
- **burn-squads/new**: Dark inputs `bg-surface-elevated`, friend selection `border-secondary bg-secondary/20`, schedule buttons dark, cancel link dark
- **profile/[uid]**: Skeleton `bg-gray-800`, back link dark, error banner dark, name `text-white`, badges translucent (`bg-primary/20`, `bg-yellow-500/20`, `bg-green-500/20`), request button `hover:bg-green-600`
- NavBar already removed from all pages in US-003 — no changes needed
- Files changed: `apps/web/src/app/(main)/burn-buddies/[id]/page.tsx`, `apps/web/src/app/(main)/burn-buddies/new/page.tsx`, `apps/web/src/app/(main)/burn-squads/[id]/page.tsx`, `apps/web/src/app/(main)/burn-squads/new/page.tsx`, `apps/web/src/app/(main)/profile/[uid]/page.tsx`
- **Learnings for future iterations:**
  - Badge colors for dark theme: use translucent backgrounds (e.g., `bg-amber-500/20 text-amber-400` instead of `bg-amber-100 text-amber-800`)
  - Edit/settings panels: `bg-surface border-gray-700` with `bg-surface-elevated` inputs
  - Selected states in lists: use `border-{color} bg-{color}/20` pattern instead of light pastels
  - Delete confirmations: `bg-surface border-red-500/30` with `text-red-400` instead of red backgrounds
  - Cancel/secondary buttons: `border-gray-600 bg-surface-elevated text-gray-300 hover:bg-gray-500/20`
---
