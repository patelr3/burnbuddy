# Ralph Progress Log
Started: Tue Mar  3 08:20:41 AM UTC 2026
---

## Codebase Patterns
- CLAUDE.md is a symlink to `.github/copilot-instructions.md` — edit the symlink target when staging
- `yarn typecheck` runs typecheck across all workspaces (web, mobile, shared, api)
- azure/login@v2 OIDC uses `client-id`, `tenant-id`, `subscription-id` as separate `with:` inputs — requires top-level `permissions: { id-token: write, contents: read }`
- deploy-api.yml and deploy-web.yml share identical OIDC login patterns — keep them in sync when making changes

## 2026-03-03T08:20Z - US-001
- Added `## Secrets & Security` section to CLAUDE.md with AKV rules, key vault names, and firebase config notes
- Files changed: CLAUDE.md (→ .github/copilot-instructions.md)
- **Learnings for future iterations:**
  - CLAUDE.md is a symlink to `.github/copilot-instructions.md` — must `git add .github/copilot-instructions.md` not `CLAUDE.md`
  - Typecheck runs cleanly across all 4 workspaces in ~7s
---

## 2026-03-03T08:22Z - US-002
- Changed deploy-prod job's `needs` from `deploy-beta` to `test-beta` in `.github/workflows/deploy-web.yml`
- Pipeline dependency chain is now: quality → deploy-beta → test-beta → deploy-prod → test-prod
- Files changed: .github/workflows/deploy-web.yml
- **Learnings for future iterations:**
  - deploy-web.yml has 4 azure/login steps (deploy-beta, test-beta, deploy-prod, test-prod) — all currently use client secret creds JSON
  - deploy-api.yml already had the correct `needs: test-beta` for deploy-prod — use it as reference for future changes
---

## 2026-03-03T08:23Z - US-003
- Replaced all 4 azure/login@v2 steps in deploy-web.yml from client secret creds JSON to OIDC inputs (client-id, tenant-id, subscription-id)
- Added top-level `permissions: { id-token: write, contents: read }` block
- Removed all AZURE_CLIENT_SECRET references from deploy-web.yml
- Files changed: .github/workflows/deploy-web.yml
- **Learnings for future iterations:**
  - OIDC format for azure/login@v2 uses `client-id`, `tenant-id`, `subscription-id` as separate `with:` inputs (not the JSON `creds` blob)
  - deploy-web.yml has 4 azure/login steps: deploy-beta, test-beta, deploy-prod, test-prod — all must be updated together
  - The `permissions:` block goes at top level (between `on:` and `env:`) to apply to all jobs
---

## 2026-03-03T08:25Z - US-004
- Replaced all 4 azure/login@v2 steps in deploy-api.yml from client secret creds JSON to OIDC inputs (client-id, tenant-id, subscription-id)
- Added top-level `permissions: { id-token: write, contents: read }` block
- Removed all AZURE_CLIENT_SECRET references from deploy-api.yml
- Files changed: .github/workflows/deploy-api.yml
- **Learnings for future iterations:**
  - deploy-api.yml and deploy-web.yml now both use identical OIDC pattern — keep them in sync for future changes
  - Both workflows have 4 azure/login steps each (deploy-beta, test-beta, deploy-prod, test-prod) — always update all 4 together
---
