# Ralph Progress Log
Started: Thu Mar  5 07:32:41 AM UTC 2026

## Codebase Patterns
- Streak calculator now uses `GroupWorkout[]` as the sole input — no more per-member individual workout queries
- Reusable `StatCard` component lives in `apps/web/src/components/StatCard.tsx` — use it for any stats grid display
- Profile endpoint aggregates stats across burn buddies and squads using `calculateStreaks()` and `calculateHighestStreakEver()` — reuse these instead of reimplementing
- For buddy relationship name display: partner's displayName (from users collection) for buddies, squad's `name` field for squads
- Friendship check uses sorted composite key `${uid1}_${uid2}` as the friends document ID
- Streak reset uses a 7-day inactivity window — gaps of 1-6 days are tolerated, 7+ resets to 0
- Route test files use `vi.hoisted` + `vi.mock` pattern with collection-keyed mock routing in `getDb().collection(name)`
- `calculateHighestStreakEver()` walks full GroupWorkout history from earliest to latest with same 7-day gap tolerance
- `calculateGroupStats()` returns GroupStats shape (highestStreakEver, firstGroupWorkoutDate, groupWorkoutsAllTime, groupWorkoutsThisMonth)
- Stats endpoints follow same auth/membership gating as streaks endpoints
- When adding a new Firestore collection to route tests, add the mock query chain in `vi.hoisted`, add the collection routing in `vi.mock`, and add `mockReturnThis()` for `.where()` in `beforeEach`
- `calculateStreaks(groupWorkouts)` returns `{ burnStreak, supernovaStreak }` — both values are always equal since GroupWorkout implies all members participated
- Web app uses `apiGet`/`apiPut`/`apiPost`/`apiPatch` from `@/lib/api` for all authenticated API calls — avoid raw `fetch`
- Next.js `redirect()` from `next/navigation` provides server-side redirects for removed pages

---

## 2026-03-05T07:38:00Z - US-001
- Refactored `calculateStreaks` to accept `GroupWorkout[]` instead of `memberUids[] + Workout[]`
- A streak day = any calendar day with a GroupWorkout for the reference
- Updated burn-buddies and burn-squads routes to query `groupWorkouts` collection by `referenceId`
- Rewrote all streak calculator tests for new signature
- Added tests: "streak increments on unscheduled workout days" and "streak increments when workouts overlap within 20 min but outside schedule"
- Updated burn-buddies and burn-squads route tests for new GroupWorkout-based streak queries
- Files changed:
  - `services/api/src/services/streak-calculator.ts`
  - `services/api/src/services/streak-calculator.test.ts`
  - `services/api/src/routes/burn-buddies.ts`
  - `services/api/src/routes/burn-buddies.test.ts`
  - `services/api/src/routes/burn-squads.ts`
  - `services/api/src/routes/burn-squads.test.ts`
- **Learnings for future iterations:**
  - GroupWorkout documents already enforce the "all members overlapping within 20 min" constraint — streak logic just counts days with GroupWorkouts
  - When refactoring streak calc, route handlers also need updating since they pass different data now
  - Route test mock structure: add new collection mock in `vi.hoisted`, wire it in `vi.mock`'s `collection()` switch, and reset `.where.mockReturnThis()` in `beforeEach`
  - The `Workout` import can be removed from routes that only used it for streak queries
---

## 2026-03-05T07:41:00Z - US-002
- Updated `calculateStreaks` to tolerate gaps of up to 6 consecutive days; 7+ day gap resets streak
- Updated existing tests whose expectations changed (gap of 2 days no longer resets, gap of 2 days between workout days still counts both)
- Added 4 new tests: streak survives 6-day gap, streak resets on 7-day gap, streak survives multiple short gaps, streak resets when no workout in last 7 days
- Files changed:
  - `services/api/src/services/streak-calculator.ts`
  - `services/api/src/services/streak-calculator.test.ts`
- **Learnings for future iterations:**
  - The streak algorithm uses a simple `gapDays` counter that resets on each workout day — clean and testable
  - Leading gap (from today backwards to first workout) also follows the 7-day rule
  - When changing streak behavior, update both the algorithm tests AND verify route tests still pass (they use the real calculator indirectly through mocked Firestore data)
---

## 2026-03-05T07:44:00Z - US-003
- Merged username editing from /settings into /account page
- Account page now has: Profile info, Username editing, Onboarding toggle, Sign Out
- Replaced /settings page content with `redirect('/account')` server-side redirect
- Removed Settings link from NavBar (only Account link remains)
- Switched username save from raw `fetch` to `apiPut` helper for consistency
- Files changed:
  - `apps/web/src/app/account/page.tsx` — added username editing section with validation
  - `apps/web/src/app/settings/page.tsx` — replaced with redirect to /account
  - `apps/web/src/components/NavBar.tsx` — removed Settings link
- **Learnings for future iterations:**
  - Use `redirect()` from `next/navigation` for server-side redirects on removed pages (keeps the URL path for bookmarks/links working)
  - The settings page used raw `fetch` instead of `apiPut` — always prefer the shared API helpers in `@/lib/api`
  - NavBar auth check: middleware handles redirect to /login for unauthenticated users, so NavBar only renders on protected pages
  - Browser testing for auth-gated pages requires login first — unauthenticated requests redirect to /login via middleware
---

## 2026-03-05T07:50:00Z - US-004
- Added `GroupStats` type to `@burnbuddy/shared` with fields: highestStreakEver (value + date), firstGroupWorkoutDate, groupWorkoutsAllTime, groupWorkoutsThisMonth
- Added `calculateHighestStreakEver()` and `calculateGroupStats()` to streak-calculator.ts
- Added `GET /burn-buddies/:id/stats` endpoint — membership gated (403 for non-members)
- Added `GET /burn-squads/:id/stats` endpoint — membership gated (403 for non-members)
- Added 6 unit tests for calculateHighestStreakEver, 2 for calculateGroupStats
- Added 5 route tests each for burn-buddies stats and burn-squads stats (401, 404, 403, empty stats, populated stats)
- Files changed:
  - `packages/shared/src/types.ts` — added GroupStats interface
  - `services/api/src/services/streak-calculator.ts` — added calculateHighestStreakEver, calculateGroupStats
  - `services/api/src/services/streak-calculator.test.ts` — added tests for new functions
  - `services/api/src/routes/burn-buddies.ts` — added stats endpoint
  - `services/api/src/routes/burn-buddies.test.ts` — added stats tests
  - `services/api/src/routes/burn-squads.ts` — added stats endpoint
  - `services/api/src/routes/burn-squads.test.ts` — added stats tests
- **Learnings for future iterations:**
  - The shared package must be rebuilt (`yarn build`) before `yarn typecheck` will find new exported types
  - Stats endpoints follow same pattern as streaks endpoints — doc lookup + membership check + GroupWorkout query + calculation
  - `calculateHighestStreakEver` walks forward through sorted dates (unlike `calculateStreaks` which walks backward from today)
  - Gap between two dates for highest-streak-ever is `(curr - prev) / DAY - 1` — be careful with off-by-one
---

## 2026-03-05T07:55:00Z - US-005
- Added `ProfileStats` type to `@burnbuddy/shared` with fields: displayName, username, highestActiveStreak, highestStreakEver, firstWorkoutDate, workoutsAllTime, workoutsThisMonth, buddyRelationshipStatus
- Added `GET /users/:uid/profile` endpoint to users.ts with friendship gating (403 for non-friends)
- Aggregates stats across all target user's burn buddy and squad relationships
- Determines burn buddy relationship status between requester and target (none, pending_sent, pending_received, buddies)
- Created `users-profile.test.ts` with 8 tests: 401, 404, 403, empty stats, populated stats with buddy relationship, pending_sent, pending_received, multi-relationship aggregation
- Files changed:
  - `packages/shared/src/types.ts` — added ProfileStats interface
  - `services/api/src/routes/users.ts` — added `/:uid/profile` endpoint with imports for BurnBuddy, BurnSquad, GroupWorkout, Workout, ProfileStats, and streak calculator functions
  - `services/api/src/routes/users-profile.test.ts` — new test file with 8 tests
- **Learnings for future iterations:**
  - The `/:uid/profile` route must be registered BEFORE `/:uid` in Express to avoid the catch-all matching first (though in Express `/:uid` only matches single-segment paths so `/uid/profile` wouldn't conflict — still good practice to keep specific routes first)
  - Profile endpoint is a read-heavy aggregation — it queries users, friends, burnBuddies (2 queries), burnSquads, partner user profiles, groupWorkouts (per relationship), workouts, and optionally burnBuddyRequests (2 queries). Consider caching for production.
  - When testing a route that calls `doc().get()` multiple times on the same collection, the `mockDocGet` mock needs `mockResolvedValueOnce` calls in the exact sequence they're called in the route handler
  - BurnBuddy uid1/uid2 are sorted, so `isBuddy` check must handle both orderings
---

## 2026-03-05T08:00:00Z - US-006
- Added stats from GroupStats API to burn buddy and burn squad detail pages
- Created reusable `StatCard` component in `apps/web/src/components/StatCard.tsx`
- Both detail pages now fetch `/burn-buddies/:id/stats` or `/burn-squads/:id/stats` in parallel with existing data
- New stat cards: Highest Streak (with date), First Workout, Total Workouts (all time), This Month (from API)
- Retained existing: Burn Streak, Supernova Streak, This Week, Buddy/Squad Age
- Graceful null-safe rendering when stats API unavailable
- Files changed:
  - `apps/web/src/components/StatCard.tsx` — new reusable component
  - `apps/web/src/app/burn-buddies/[id]/page.tsx` — fetch stats, use shared StatCard, display new fields
  - `apps/web/src/app/burn-squads/[id]/page.tsx` — same changes as buddies page
- **Learnings for future iterations:**
  - `GroupStats` type is imported from `@burnbuddy/shared` — make sure shared package is built first
  - Detail pages fetch stats in parallel with streaks and group workouts using `Promise.all` with `.catch(() => null)` for graceful degradation
  - `StatCard` component uses optional `unit` and `colorClass` props — pass `colorClass` for themed values, omit `unit` for date-only cards
  - Browser verification requires auth — unauthenticated requests redirect to /login
- `getNextWorkout(schedule)` calculates the next upcoming workout from a `WorkoutSchedule` — returns `null` if no schedule or no upcoming day within the week
- Listing cards on home page use `CombinedItem` interface — extend it when adding new fields to cards

---

## 2026-03-05T08:03:00Z - US-007
- Created friend profile page at `/profile/[uid]` using `GET /users/:uid/profile` API
- Displays: displayName, username, highest active streak (with buddy/squad name), highest streak ever (with date + name), first workout date, total workouts all time, total workouts this month
- "Request Burn Buddy" button shows when `buddyRelationshipStatus === 'none'`, hidden for buddies/pending
- Status badges for buddies, pending_sent, pending_received states
- Updated friends page: friend list items are now clickable via `<Link>` to `/profile/[uid]`
- Reused `StatCard` component from US-006 for stats grid
- Loading and error states handled (404 user not found, 403 not friends)
- Files changed:
  - `apps/web/src/app/profile/[uid]/page.tsx` — new friend profile page
  - `apps/web/src/app/friends/page.tsx` — made friend items clickable with Link to profile
- **Learnings for future iterations:**
  - Profile page uses `apiGet<ProfileStats>` — the ProfileStats type from @burnbuddy/shared has all needed fields
  - `buddyRelationshipStatus` from the profile API drives the burn buddy button visibility — no extra API calls needed
  - Friends page friend items wrap the info area in `<Link>` while keeping action buttons outside the link to prevent navigation on button clicks
  - Error messages from API use status code in the error message string (e.g. `msg.includes('403')`) — matches the `apiGet` helper's error format
---

## 2026-03-05T08:05:00Z - US-008
- Enhanced burn buddy and squad listing cards with fire emoji streak display (🔥12)
- Added next planned workout display derived from WorkoutSchedule (e.g., Next: Mon 7:00 AM)
- Next workout field hidden entirely when no upcoming scheduled workout exists
- Created `getNextWorkout()` helper to calculate next occurrence from schedule day/time
- Removed "streak" label text under the number (replaced by fire emoji for cleaner look)
- Files changed:
  - `apps/web/src/app/page.tsx` — added WorkoutSchedule import, getNextWorkout helper, extended CombinedItem, updated card rendering
- **Learnings for future iterations:**
  - `WorkoutSchedule.days` uses 3-letter day names ('Mon', 'Tue', etc.) matching JS `toLocaleDateString` short names
  - `WorkoutSchedule.time` is optional and in "HH:MM" 24-hour format when present
  - BurnBuddy has `workoutSchedule` directly; BurnSquad has it nested in `settings.workoutSchedule`
  - The `CombinedItem` interface is the centralized shape for listing cards — add any new card fields there
  - Fire emoji is rendered as inline text, not as an image — works cross-platform
---
