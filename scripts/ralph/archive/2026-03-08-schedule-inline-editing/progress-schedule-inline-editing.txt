# Ralph Progress Log — Schedule Inline Editing
Started: 2026-03-08

## Codebase Patterns
- Auth bypass for local browser testing: set `auth_session=1` cookie on localhost domain
- Skeleton components should match the main component layout — update both when restructuring
- Schedule box uses `border-primary/30 bg-primary/10` for active schedules, `border-dashed border-gray-600` for empty placeholders
- Reuse existing state variables (editing, selectedDays, scheduleTime) when refactoring edit triggers
- Web typecheck has pre-existing errors (missing module declarations); only check for new errors in your changed files
- Squad pages use violet colors (`violet-500`) instead of primary/orange for schedule boxes and editor
- When a page has both a settings panel AND inline schedule editing, use separate state variables (isEditingSchedule vs editing)

---

## 2026-03-08T22:17Z - US-001
- Implemented inline schedule editing for Burn Buddies detail page
- Removed "Edit Schedule" button from page header
- Added dashed placeholder box (+ icon, "Add a schedule" text, ▾ chevron) when no schedule exists
- Made orange schedule info box clickable with hover effect and chevron (▾/▴) toggle
- Inline editor now expands below the schedule box/placeholder (not a separate panel)
- Added Cancel button alongside Save Schedule button
- "Add to Calendar" remains visible when editor is open
- Updated BurnBuddySkeleton to remove header button placeholder and add schedule skeleton
- Files changed: `apps/web/src/app/(main)/burn-buddies/[id]/page.tsx`
- **Learnings for future iterations:**
  - The page uses `useAuth()` loading state + `useBurnBuddy()` data loading — skeleton appears when data is loading
  - Without Firebase credentials locally, pages show skeleton forever — use `auth_session=1` cookie to bypass middleware redirect
  - The `flex items-center justify-between` on header div should be changed to `flex items-center` when removing the right-side button
  - Schedule box uses a `<button>` element for semantics (cursor-pointer, clickable), not a `<div>`
---

## 2026-03-08T22:45Z - US-002
- Implemented inline schedule editing for Burn Squads detail page
- Added separate `isEditingSchedule` state independent from settings panel `editing` state
- Added `inlineScheduleDays`, `inlineScheduleTime`, `inlineSaving`, `inlineTimeError` state
- Added `handleSaveInlineSchedule` and `toggleInlineDay` functions
- Dashed placeholder (admin-only) with '+' icon and chevron when no schedule exists
- Non-admins see nothing when no schedule; see non-clickable violet box when schedule exists
- Admin-only clickable violet schedule box with hover effect and chevron (▾/▴)
- Inline editor with violet-themed day toggles + time input, Save + Cancel buttons
- Settings panel (Edit Settings button) completely preserved and unchanged
- Add to Calendar button remains visible when inline editor is open
- Updated BurnSquadSkeleton with schedule placeholder
- Files changed: `apps/web/src/app/(main)/burn-squads/[id]/page.tsx`
- **Learnings for future iterations:**
  - Squad schedule is nested under `squad.settings.workoutSchedule` (not top-level like burn buddy)
  - The settings panel save (`handleSaveSettings`) sends name + all settings; inline save sends name + settings preserving existing values
  - Must use separate state variables for inline editor to avoid conflicts with settings panel state
  - The outer condition `(isAdmin || hasSchedule)` controls visibility; inner conditions control interactivity
---
