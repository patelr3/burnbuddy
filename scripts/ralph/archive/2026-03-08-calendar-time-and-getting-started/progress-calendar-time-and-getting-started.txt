# Ralph Progress Log — Required Workout Time & Getting Started Card Update
Started: 2026-03-08

## Codebase Patterns
- Shared package build requires `packages/shared/src/globals.d.ts` for console type declarations (no @types/node)
- After changing packages/shared/src/types.ts, must run `cd packages/shared && yarn build` before downstream typechecks
- Web schedule constructors use `scheduleTime || ''` pattern for required time field
- API typecheck: `cd services/api && npx tsc --noEmit`; Web typecheck: `cd apps/web && npx tsc --noEmit`
- Must run `yarn install --frozen-lockfile` in worktree before typechecks work (node_modules not shared)
---

## 2026-03-08T07:42 - US-001
- Made `time` field required in `WorkoutSchedule` interface (`time?: string` → `time: string`)
- Fixed 3 TypeScript errors in web app where `time` could be `undefined`
- Added `packages/shared/src/globals.d.ts` to fix pre-existing shared package build issue (console types)
- Files changed:
  - `packages/shared/src/types.ts` — made time required
  - `packages/shared/src/globals.d.ts` — new file for console type declarations
  - `apps/web/src/app/(main)/burn-buddies/[id]/page.tsx` — use `''` instead of `undefined` for time
  - `apps/web/src/app/(main)/burn-squads/[id]/page.tsx` — same fix
  - `apps/web/src/app/(main)/burn-squads/new/page.tsx` — include time in schedule literal
---

## 2026-03-08T08:01 - US-002
- Added server-side validation for required workout time in 3 API endpoints:
  - PUT /burn-buddies/:id — rejects workoutSchedule without time
  - POST /burn-squads — rejects workoutSchedule without time
  - PUT /burn-squads/:id/settings — rejects workoutSchedule without time
- Added 8 new test cases across burn-buddies.test.ts and burn-squads.test.ts
- All 359 tests pass (was 351), typecheck passes
- Files changed:
  - `services/api/src/routes/burn-buddies.ts` — added time validation before update
  - `services/api/src/routes/burn-squads.ts` — added time validation in POST and PUT settings
  - `services/api/src/routes/burn-buddies.test.ts` — 2 new tests (missing time, empty time)
  - `services/api/src/routes/burn-squads.test.ts` — 6 new tests (missing/empty time for POST and PUT settings, plus success cases)
- **Learnings for future iterations:**
  - Validation pattern: check `if (schedule && (!schedule.time || schedule.time.trim() === ''))` — handles undefined, null, and empty string
  - PUT /burn-buddies/:id is the schedule update endpoint (no /schedule sub-path)
  - Burn squad schedule lives in `settings.workoutSchedule`, burn buddy schedule is at root `workoutSchedule`
  - Calendar endpoint tests use mocked Firestore data (no time field) to test all-day events — these are read-path tests and don't need time validation
- Web schedule editor: `canSaveSchedule` derived boolean controls Save button disabled state, `timeError` state controls inline validation
---

## 2026-03-08T08:20 - US-003
- Made time required in web Burn Buddy schedule editor:
  - Changed label from "Time (optional):" to "Time:"
  - Save Schedule button disabled until both days and time are provided (`canSaveSchedule` computed boolean)
  - Inline validation "Please select a workout time" shown below time input when user attempts save without time
  - Validation error clears when user enters a time value or toggles edit mode
- Files changed:
  - `apps/web/src/app/(main)/burn-buddies/[id]/page.tsx` — added `timeError` state, `canSaveSchedule` computed, validation UI
- **Learnings for future iterations:**
  - Pattern for required field validation in schedule editor: `canSaveSchedule` boolean + `timeError` state + early return in handleSave
  - Local dev doesn't have Firebase Auth configured — login form doesn't render. Use beta environment for browser testing
  - Beta login uses FirebaseUI (Sign in with email / Google flow), not a simple email+password form
---

## 2026-03-08T08:30 - US-004
- Made time required in web Burn Squad creation and settings forms:
  - Squad creation (`burn-squads/new/page.tsx`): added `timeError` state, `canSubmit` computed boolean (requires time when days selected), early return validation in `handleSubmit`, inline error message
  - Squad settings (`burn-squads/[id]/page.tsx`): added `timeError` state, `canSaveSettings` computed boolean, early return validation in `handleSaveSettings`, inline error message, clears timeError on edit toggle
  - Changed time labels from "Time (optional)" to "Time:" in both forms
- Files changed:
  - `apps/web/src/app/(main)/burn-squads/new/page.tsx` — creation form validation
  - `apps/web/src/app/(main)/burn-squads/[id]/page.tsx` — settings form validation
- **Learnings for future iterations:**
  - Squad creation uses `canSubmit` (not `canSaveSchedule`) since it combines squad name + schedule validation
  - Squad settings uses `canSaveSettings` for same reason (name + schedule)
  - Browser testing requires beta auth — can't verify locally without Firebase Auth config
---

## 2026-03-08T08:24 - US-005
- Made time required in all three mobile schedule forms:
  - BurnBuddyDetailScreen: Added full schedule editor (was view-only) with Edit button, day selection, required time input, and Save Schedule button
  - NewBurnSquadScreen: Added timeError state, canSubmit computed boolean, validation in handleSubmit, inline error, disabled submit without time
  - BurnSquadDetailScreen: Added timeError state, canSaveSettings computed boolean, validation in handleSaveSettings, inline error, disabled save without time
- Fixed 2 pre-existing TypeScript errors caused by US-001's required time field
- Changed time labels from "Time (optional):" to "Time:" in both squad screens
- All typechecks pass (mobile, API, web), all 359 API tests pass
- Files changed:
  - apps/mobile/src/screens/BurnBuddyDetailScreen.tsx — added schedule editor with required time
  - apps/mobile/src/screens/NewBurnSquadScreen.tsx — time validation + typecheck fix
  - apps/mobile/src/screens/BurnSquadDetailScreen.tsx — time validation + typecheck fix
- **Learnings for future iterations:**
  - BurnBuddyDetailScreen was view-only — needed Edit button + schedule editing added (unlike web which already had it)
  - Mobile uses apiPut from lib/api.ts for PUT requests, same pattern as web
  - Burn buddy schedule PUT payload: { workoutSchedule: { days: [...], time: '...' } } at root level
  - Pattern: canSave computed boolean + timeError state + early return in save handler + onChangeText clears error
  - Burn buddy uses orange theme (#E05A00), burn squad uses purple theme (#8b5cf6) consistently in mobile
---

## 2026-03-08T08:23 - US-006
- Rewrote Getting Started card with two numbered steps:
  1. Add a friend — search by email on the Friends page
  2. Request a Burn Buddy — once accepted, go to Burn Buddies to start tracking
- Removed all mention of Burn Squads from the card
- Dismiss button (×) left unchanged
- Typecheck passes
- Files changed:
  - `apps/web/src/components/GettingStartedCard.tsx` — rewrote card content
- **Learnings for future iterations:**
  - Getting Started card is a simple presentational component with onDismiss prop — no state management needed
  - Beta login required for browser verification; local dev lacks Firebase Auth config
  - Card dismissal and re-enable toggle are handled by the parent (dashboard), not the card itself
---

## 2026-03-08T08:25 - US-007
- Rewrote mobile Getting Started card with two numbered steps matching web flow:
  1. Add a friend — search by email on the Friends screen
  2. Request a Burn Buddy — once accepted, go to Burn Buddies to start tracking
- Fixed branding from 'buddyburn' to 'BurnBuddy'
- Removed all Burn Squad mentions from the card
- Mobile typecheck passes, all 359 API tests pass
- Files changed:
  - `apps/mobile/src/components/GettingStartedCard.tsx` — rewrote card content
- **Learnings for future iterations:**
  - Mobile GettingStartedCard uses same pattern as web — presentational component with onDismiss prop
  - Mobile uses React Native Text with {'\n\n'} for line breaks (not <br/> or <ol>)
  - Keep mobile and web Getting Started cards in sync when updating onboarding copy
---
