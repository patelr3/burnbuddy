# Ralph Progress Log
Started: Thu Mar  5 05:04:23 PM UTC 2026

## Codebase Patterns
- Streak calculator uses `GroupWorkout[]` as input — `calculateStreaks(groupWorkouts)` returns `{ burnStreak, supernovaStreak }`
- Route test files use `vi.hoisted` + `vi.mock` pattern with collection-keyed mock routing in `getDb().collection(name)`
- Web app uses `apiGet`/`apiPost`/`apiPut`/`apiPatch` from `@/lib/api` for all authenticated API calls
- Reusable `StatCard` component lives in `apps/web/src/components/StatCard.tsx`
- Friendship check uses sorted composite key `${uid1}_${uid2}` as the friends document ID
- Streak reset uses a 7-day inactivity window
- `calculateGroupStats()` returns GroupStats shape
- When adding a new Firestore collection to route tests, add the mock query chain in `vi.hoisted`, add the collection routing in `vi.mock`, and add `mockReturnThis()` for `.where()` in `beforeEach`

---
