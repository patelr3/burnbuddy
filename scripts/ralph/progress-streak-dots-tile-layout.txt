# Ralph Progress Log — Streak Dots Tile Layout
Started: 2026-03-08

## Codebase Patterns
- StreakDayInfo requires `groupWorkoutId: string | null` field — don't omit when creating placeholder data
- Mobile StreakDots tile uses inline `width: '47%'` matching statCard style — no grid container needed, statsGrid uses flexWrap
- StatCard styling: `rounded-lg border border-gray-700 bg-surface px-4 py-3.5`
- Web typecheck: `cd packages/shared && yarn build && cd ../../apps/web && yarn typecheck`
- EMPTY_LAST_7_DAYS constant exists in profile page for fallback — reuse pattern for empty states

---

## 2026-03-08T22:20 - US-001
- Restyled web StreakDots component as a card tile matching StatCard styling
- Stacked layout: label+count on top row, 7 dots on bottom row
- Removed day-of-week labels (S, M, T, W, etc.) below dots
- Added EMPTY_DAYS fallback for when last7Days is empty (shows 0 count, all ○ dots)
- Preserved danger state (red) and both orange/violet color variants
- Files changed: `apps/web/src/components/StreakDots.tsx`
- **Learnings for future iterations:**
  - StreakDayInfo type requires `groupWorkoutId` field — placeholder arrays must include it or typecheck fails
  - isDangerState must handle empty arrays (return false) to avoid false positives
---

## 2026-03-08T22:25 - US-002
- Integrated web streak tiles into the 2-column stat grid on all 3 pages
- Removed separate `mb-5 flex flex-col gap-2` StreakDots wrappers
- Placed both StreakDots as first two children inside existing `grid grid-cols-2 gap-3` grids
- Each streak tile occupies one grid cell (single column, not spanning)
- No conditional wrapping — StreakDots always renders (empty state handled by component)
- Files changed:
  - `apps/web/src/app/(main)/burn-buddies/[id]/page.tsx`
  - `apps/web/src/app/(main)/burn-squads/[id]/page.tsx`
  - `apps/web/src/app/(main)/profile/[uid]/page.tsx`
- **Learnings for future iterations:**
  - The change is purely structural — remove the flex-col wrapper and move StreakDots into the grid div
  - Profile page uses a different data source (profile.highestActiveStreak) vs detail pages (streaks.burnStreak)
  - Browser verification requires auth — use beta environment for visual verification of authenticated pages
---

## 2026-03-08T22:27 - US-003
- Restyled mobile StreakDots component as a card tile matching statCard styling
- Added EMPTY_DAYS fallback with groupWorkoutId: null for empty state
- Stacked layout: label+count on top, 7 dots on bottom row
- Removed day-of-week labels below dots
- Tile style: backgroundColor '#fafafa', borderRadius 10, padding 14, borderWidth 1, borderColor '#f1f5f9', width '47%'
- Files changed: `apps/mobile/src/components/StreakDots.tsx`
- **Learnings for future iterations:**
  - Mobile statCard uses percentage width ('47%') instead of grid — StreakDots tile needs same width
  - isDangerState needs empty array guard (return false) same as web version
---

## 2026-03-08T22:27 - US-004
- Integrated mobile streak tiles into statsGrid on BurnBuddyDetailScreen and BurnSquadDetailScreen
- Removed conditional `streaks.last7Days.length > 0` wrapping and streakDotsSection wrapper
- StreakDots now renders as first two children inside statsGrid View
- Cleaned up unused streakDotsSection style from both files
- Files changed:
  - `apps/mobile/src/screens/BurnBuddyDetailScreen.tsx`
  - `apps/mobile/src/screens/BurnSquadDetailScreen.tsx`
- **Learnings for future iterations:**
  - Mobile statsGrid uses flexWrap with percentage widths — StreakDots tiles integrate naturally
  - Always clean up unused styles after removing wrapper Views
---
