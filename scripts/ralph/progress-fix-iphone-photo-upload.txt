# Ralph Progress Log — Fix iPhone Photo Upload & Animify Timeout
Started: 2026-03-08

## Codebase Patterns
- `anime-filter.ts` operates on in-memory buffers; pre-downscale input before parallel pipeline steps for performance
- API typecheck requires `cd packages/shared && yarn build` first (resolves @burnbuddy/shared from dist/)
- Run API tests with `cd services/api && yarn test -- <test-file>`

---

## 2026-03-08T02:24 - US-001
- Implemented early downscale in anime filter: cap input to 1024px on longest side before posterize/edge-detection pipeline
- Added MAX_PROCESSING_SIZE constant (1024) and pre-downscale step using sharp resize
- Parallel pipeline steps now operate on the pre-downscaled buffer instead of re-reading from inputBuffer
- Added performance test: 4032×3024 JPEG completes in ~278ms (well under 10s limit)
- Files changed: `services/api/src/lib/anime-filter.ts`, `services/api/src/lib/anime-filter.test.ts`
- **Learnings for future iterations:**
  - The parallel pipeline (posterize, edgesX, edgesY) previously each re-decoded from inputBuffer with .rotate() — pre-downscaling into a single buffer avoids 3× redundant decode+resize
  - sharp().resize() with fit:'cover' preserves aspect ratio while covering the target dimensions
  - iPhone photos can be 4032×3024 (12MP) to 8064×6048 (48MP) — always cap resolution for image processing pipelines
---

## 2026-03-08T02:25 - US-002
- Added 'image/heic' and 'image/heif' to ALLOWED_IMAGE_TYPES in users.ts
- Added two unit tests: one for HEIC uploads, one for HEIF uploads (both verify 200 response)
- Files changed: `services/api/src/routes/users.ts`, `services/api/src/routes/users-profile-picture.test.ts`
- **Learnings for future iterations:**
  - Multer checks mimetype header, not actual file bytes — tests can use any buffer with the correct contentType
  - Sharp 0.34.5 prebuilt binaries include HEIF decode support, no extra system packages needed
  - ALLOWED_IMAGE_TYPES and the error message string are separate — update both if you want to list accepted types in the error
---

## 2026-03-08T02:41 - US-003
- Added 'image/heic' and 'image/heif' to ACCEPTED_IMAGE_TYPES constant in account page
- The hidden file input's accept attribute uses the constant, so it automatically picks up the new types
- Verified via Next.js build output that the compiled JS includes the HEIC/HEIF types in the accept attribute
- Files changed: `apps/web/src/app/(main)/account/page.tsx`
- **Learnings for future iterations:**
  - Account page requires Firebase auth — browser verification of authenticated pages needs login flow
  - Next.js dev mode doesn't pre-build chunks; use `npx next build` then grep `.next/static/chunks/` to verify compiled output
  - The ACCEPTED_IMAGE_TYPES constant is on line 15 of account/page.tsx and is used by the hidden file input on line 284
---
